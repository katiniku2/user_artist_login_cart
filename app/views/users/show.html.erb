<% provide(:title, @user.name) %>
<div class="row">
	<aside class="col-md-4">
		<section class="user_info">
			<h1>
				<%= gravatar_for @user %>
				<%= @user.name %>
				<p><%= session.to_h %></p>
			</h1>
		</section>
	</aside>
	<%= link_to '商品一覧', cds_path %>
</div>